{
  "buildCommand": "pnpm --filter=@applore/db generate && pnpm --filter=@applore/web build",
  "installCommand": "HUSKY=0 pnpm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
